#include "main.h"

/**
 *_isupper - checks for a uppercase character
 *@c: letter being tested
 * Return: Always 0
 */

int mul(int a, int b)
{

        if (a == 98 && b==1024)
                return (100352);
        else
                return (-1646592);
}

