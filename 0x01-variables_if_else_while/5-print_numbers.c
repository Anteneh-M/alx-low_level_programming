#include <stdio.h>

/**
* main - Prints the alphabet in lowercase except q and e.
*
* Return: Always 0.
*/
int main(void)
{
	int n;

	for (n = 0; letter <= 10;  n++)
	
	putchar(n);

	putchar('\n');

	return (0);
}
