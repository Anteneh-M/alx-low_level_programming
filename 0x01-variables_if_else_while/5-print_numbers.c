#include <stdio.h>

/**
* main - Prints the alphabet in lowercase except q and e.
*
* Return: Always 0.
*/
int main(void)
{
	int n;

	for (n = 0; n <= 10;  n++)
		{	
		printf("%d", n);
		}	
	putchar('\n');
	return 0;
}
