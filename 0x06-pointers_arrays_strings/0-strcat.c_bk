#include <stdio.h>
#include <string.h>
#include "main.h"

char *_strcat(char *dest, char *src)
{
/*	char *dest = "Hello";
/*	char *src = "World"; */
	strcat(*dest,*src);
	printf("Concatenated String: %s\n", *dest);
	return 0;
}
